events { worker_connections 1024; }

http {
    upstream api_servers {
        server api01:8080;
        server api02:8080;
    }

    server {
        listen 9999;

        location / {
            proxy_pass http://api_servers;
        }
    }
}